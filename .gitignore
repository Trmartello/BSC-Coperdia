node_modules
.turbo
.next
dist
*.env.local
.env
.env.production
prisma/migrations/*.sql
coverage
.DS_Store

# Arquivos enviados pelos usuários (anexos de comentários) — não versionar
apps/api/uploads/
